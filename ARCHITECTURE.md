# Star5 Timesheet Backend - Architecture Overview

## 🏗️ Arquitectura del Sistema

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React/Vue)                     │
│                     (Separate Repository)                        │
└────────────────────────┬────────────────────────────────────────┘
                         │ HTTP/REST API
                         │ JWT Auth
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                      EXPRESS.JS SERVER                           │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    Middleware Layer                       │  │
│  │  • Helmet (Security)                                      │  │
│  │  • CORS                                                   │  │
│  │  • Body Parser                                            │  │
│  │  • Morgan (Logging)                                       │  │
│  │  • Authentication Middleware (JWT)                        │  │
│  │  • Authorization Middleware (Roles)                       │  │
│  │  • Error Handler                                          │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                     Routes Layer                          │  │
│  │                                                            │  │
│  │  /api/v1/auth           (Register, Login, Profile)       │  │
│  │  /api/v1/time-entries   (CRUD Time Entries)              │  │
│  │  /api/v1/timesheets     (Timesheet Management)           │  │
│  │  /api/v1/projects       (Project Management)             │  │
│  │  /api/v1/clients        (Client Management)              │  │
│  │  /api/v1/users          (User Management)                │  │
│  │  /api/v1/reports        (Reports & Analytics)            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                   Controllers Layer                       │  │
│  │                                                            │  │
│  │  • auth.controller.ts                                     │  │
│  │  • timeEntry.controller.ts                                │  │
│  │  • timesheet.controller.ts                                │  │
│  │  • project.controller.ts                                  │  │
│  │  • user.controller.ts                                     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    Prisma ORM Layer                       │  │
│  │                                                            │  │
│  │  • Type-safe database queries                             │  │
│  │  • Automatic migrations                                   │  │
│  │  • Relations & Joins                                      │  │
│  └──────────────────────────────────────────────────────────┘  │
└───────────────────────────┬──────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      PostgreSQL Database                         │
│                                                                  │
│  Tables:                                                         │
│  • users                    • clients                            │
│  • departments              • brands                             │
│  • projects                 • campaigns                          │
│  • tasks                    • time_entries                       │
│  • timesheets               • timesheet_approvals                │
└─────────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow - Time Entry Creation

```
1. User (Employee) → Frontend UI
                      │
                      ▼
2. POST /api/v1/time-entries
   {
     projectId, taskId, date, hours, notes
   }
                      │
                      ▼
3. Express Router → authenticate middleware
                      │ (Verify JWT)
                      ▼
4. timeEntry.controller.ts
   • Validate data
   • Check project & task exist
   • Check permissions
                      │
                      ▼
5. Prisma ORM
   • prisma.timeEntry.create()
   • Auto-link to user
   • Include relations
                      │
                      ▼
6. PostgreSQL
   • INSERT INTO time_entries
   • Return created record
                      │
                      ▼
7. Response to Frontend
   {
     message: "Time entry created",
     timeEntry: { ... }
   }
```

## 🔄 Timesheet Approval Workflow

```
┌──────────────┐
│   EMPLOYEE   │
└──────┬───────┘
       │ 1. Adds time entries during week
       ▼
┌──────────────┐
│    DRAFT     │ ← Timesheet created automatically
└──────┬───────┘
       │ 2. Submits for approval
       ▼
┌──────────────┐
│  SUBMITTED   │
└──────┬───────┘
       │ 3. Manager reviews
       │
       ├─────────────┬─────────────┐
       ▼             ▼             ▼
┌──────────┐   ┌──────────┐   ┌──────────┐
│ APPROVED │   │ REJECTED │   │ PENDING  │
└──────────┘   └────┬─────┘   └──────────┘
                    │ 4. Employee
                    │    fixes issues
                    ▼
              ┌──────────────┐
              │ Back to DRAFT│
              └──────────────┘
```

## 🔐 Authentication & Authorization Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                        LOGIN REQUEST                             │
│  POST /api/v1/auth/login                                        │
│  { email, password }                                             │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   auth.controller.ts                             │
│  1. Find user by email                                           │
│  2. Verify password (bcrypt)                                     │
│  3. Check user status (ACTIVE)                                   │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Generate JWT Token                            │
│  Payload: { userId, email, role }                               │
│  Secret: JWT_SECRET from .env                                    │
│  Expires: 7 days (configurable)                                  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Return to Client                             │
│  { user, token, refreshToken }                                   │
└─────────────────────────────────────────────────────────────────┘

                    ┌─────────────────┐
                    │  For Protected  │
                    │    Endpoints    │
                    └────────┬────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              Request with Authorization Header                   │
│  Authorization: Bearer <JWT_TOKEN>                              │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                 authenticate Middleware                          │
│  1. Extract token from header                                    │
│  2. Verify token signature                                       │
│  3. Check expiration                                             │
│  4. Attach user to req.user                                      │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                 authorize Middleware (Optional)                  │
│  Check if req.user.role matches required role                   │
│  e.g., authorize('MANAGER', 'SUPER_ADMIN')                      │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              Controller Function Executes                        │
└─────────────────────────────────────────────────────────────────┘
```

## 🎯 Role-Based Access Control (RBAC)

```
┌────────────────────────────────────────────────────────────────┐
│                         USER ROLES                              │
├────────────────┬───────────────────────────────────────────────┤
│ EMPLOYEE       │ • View/Create own time entries                │
│                │ • Submit timesheets                            │
│                │ • View assigned projects                       │
├────────────────┼───────────────────────────────────────────────┤
│ MANAGER        │ EMPLOYEE permissions +                         │
│                │ • Approve/Reject timesheets                    │
│                │ • View team's time entries                     │
│                │ • Create/Edit projects                         │
│                │ • View reports                                 │
├────────────────┼───────────────────────────────────────────────┤
│ FINANCE_ADMIN  │ EMPLOYEE permissions +                         │
│                │ • View all financial reports                   │
│                │ • Export data                                  │
│                │ • Manage rates                                 │
│                │ • Create/Edit projects                         │
├────────────────┼───────────────────────────────────────────────┤
│ SUPER_ADMIN    │ ALL permissions +                              │
│                │ • Delete projects                              │
│                │ • Manage users                                 │
│                │ • System configuration                         │
└────────────────┴───────────────────────────────────────────────┘
```

## 📈 Database Schema - Key Relations

```
User (users)
├─ has many → TimeEntry (time_entries)
├─ has many → Timesheet (timesheets)
├─ belongs to → Department
├─ has many → TimesheetApproval (as approver)
└─ has many → TimesheetApproval (as submitter)

Client (clients)
├─ has many → Brand
└─ has many → Project

Brand (brands)
├─ belongs to → Client
└─ has many → Campaign

Campaign (campaigns)
├─ belongs to → Brand
└─ has many → Project

Project (projects)
├─ belongs to → Client
├─ belongs to → Campaign (optional)
├─ has many → Task
└─ has many → TimeEntry

Task (tasks)
├─ belongs to → Project
├─ belongs to → Department
└─ has many → TimeEntry

TimeEntry (time_entries)
├─ belongs to → User
├─ belongs to → Project
├─ belongs to → Task
└─ belongs to → Timesheet (optional)

Timesheet (timesheets)
├─ belongs to → User
├─ has many → TimeEntry
└─ has many → TimesheetApproval

TimesheetApproval (timesheet_approvals)
├─ belongs to → Timesheet
├─ belongs to → User (approver)
└─ belongs to → User (submitter)
```

## 🚀 API Response Format

### Success Response
```json
{
  "message": "Operation successful",
  "data": { ... },
  "metadata": {
    "timestamp": "2024-01-15T10:30:00Z",
    "count": 10
  }
}
```

### Error Response
```json
{
  "error": "Error Type",
  "message": "Detailed error message",
  "statusCode": 400,
  "timestamp": "2024-01-15T10:30:00Z"
}
```

## 🔧 Environment Variables

```
NODE_ENV              → development | production
PORT                  → Server port (default: 3000)
DATABASE_URL          → PostgreSQL connection string
JWT_SECRET            → Secret key for JWT signing
JWT_EXPIRES_IN        → Token expiration (default: 7d)
JWT_REFRESH_EXPIRES_IN → Refresh token expiration (default: 30d)
CORS_ORIGIN           → Allowed origins (comma-separated)
DEFAULT_PAGE_SIZE     → Pagination default (default: 20)
MAX_PAGE_SIZE         → Pagination max (default: 100)
```

## 📦 Dependencies Overview

### Production Dependencies
- **express**: Web framework
- **@prisma/client**: Database ORM client
- **bcryptjs**: Password hashing
- **jsonwebtoken**: JWT authentication
- **cors**: Cross-origin resource sharing
- **helmet**: Security headers
- **morgan**: HTTP request logger
- **cookie-parser**: Parse cookies
- **dotenv**: Environment variables
- **express-validator**: Request validation

### Development Dependencies
- **typescript**: Type safety
- **ts-node**: TypeScript execution
- **nodemon**: Auto-restart on changes
- **prisma**: Database toolkit
- **@types/***: TypeScript definitions

---

© 2024 Star5 Agency - Timesheet Management System
```
